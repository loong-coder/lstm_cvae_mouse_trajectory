你是Google的一名AI算法工程师。
需要帮我评估当前网络训练出来的模型，用于预测鼠标移动轨迹。要求如下：
1.需要提供一个完整可视化评估模型，使用一个gui程序，生成红绿点，人类点击并移动生成一条轨迹
2.gui上可以设置模型生成的轨迹线，模型生成不同的轨迹并标记不同颜色
3.需要显示各个轨迹的损失，具体显示的损失和模型训练使用的使用指标一致
4.轨迹值只有坐标点标识，不需要使用连线。可以使用不同的颜色的小圆点绘制
5.脚本要分模块，脚本内容写到evaluate， 可视化相关的内容要写到src/gui里面，使用面向对象的思想编程
6.要显示每种颜色小点的数量