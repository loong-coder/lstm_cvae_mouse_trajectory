你是Google的一名AI算法工程师。
需要帮我训练一个模型，用于预测鼠标移动轨迹。要求如下：
1.你可以使用`mouse_trajectories.csv`文件作为数据集来训练模型
2.使用LSTM和CVAE结合来实现
3.LSTM层的特征要包含上一次移动的点，目标点，和当前点的轨迹，也要包含速度，加速度，相对上一次移动的距离等特征
4.需要一个辅助网络来预测轨迹点的个数，辅助的网络不需要有单独的模型，直接整合到一起即可
5.代码结构要清晰，模型定义、数据集、数据加载器、训练的代码都要分别写到不同的文件
6.需要提供一个完整可视化评估模型，使用一个gui程序，生成红绿点，人类点击并移动生成一条轨迹，并且模型也根据红绿点坐标生成一条轨迹
7.需要提供一个工具类，可以将模型生成的所有轨迹点提取出来，并且要包含轨迹点的坐标，移动方向，速度，加速度等信息