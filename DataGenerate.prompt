你是Google的一名资深的Python开发工程师。
帮我生成一个鼠标轨迹收集程序， 用来收集数据移动轨迹，要求如下：
1. 使用python开发一个gui程序，先生成一个绿色的原点，当鼠标点击了绿色点后生成一个红色的原点，并开始记录鼠标轨迹，当鼠标移动到红色点并点击后完成一组数据的记录。并开始下一组数据的记录工作。
2. 数据轨迹的数据以组的形式记录，一组数据有相同的组ID，组ID递增。
3. 同一组的数据内部要有一个序列号， 由1开始递增。
4. 同一组数据的每条记录， 都要记录起点坐标，终点坐标， 当前坐标， 并且记录当前坐标的速度，加速度，运动方向， 时间戳—从起点移动到当前位置的时间间隔。
5. 起点坐标是上一次记录的当前坐标点， 第一个轨迹点的起点坐标和当前坐标一样
6. 关闭GUI后，能够从最后一次记录的组ID继续递增
7. 生成的红绿点需要随机